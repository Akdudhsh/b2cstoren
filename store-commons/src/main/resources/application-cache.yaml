spring:
  cache:
    type: redis
  redis:
    host: 192.168.68.129
    port: 6380
    jedis: # 设置Redis连接池
      pool:
        max-wait: 2000ms
        min-idle: 2
        max-idle: 8
        max-active: 10